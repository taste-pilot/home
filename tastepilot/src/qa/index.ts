/**
 * Automated visual QA: screenshots at desktop/tablet/mobile, overflow and
 * asset checks, theme + reduced-motion verification, and a machine-readable
 * qa-report.json. Arrives in milestone M9.
 */

export function runQa(_publicationDir: string): Promise<void> {
  throw new Error("not implemented yet: visual QA arrives in milestone M9");
}
