/**
 * The renderer is DETERMINISTIC: the same Semantic Document, Canon, Art
 * Direction Plan, and Artwork Manifest always produce byte-identical output.
 * Exported publications are framework-free semantic HTML with CSS custom
 * properties and tiny vanilla JS. Implementation arrives in milestone M4.
 */

export function renderPublication(_inputs: unknown): Promise<void> {
  throw new Error("not implemented yet: the renderer arrives in milestone M4");
}
